# Local override — sets correct gateway URL for native (non-Docker) OpenClaw

services:
  mission-control:
    environment:
      OPENCLAW_GATEWAY_URL: ws://host.docker.internal:18789
    extra_hosts:
      - "host.docker.internal:host-gateway"    # Linux shim; no-op on Docker Desktop for Mac
